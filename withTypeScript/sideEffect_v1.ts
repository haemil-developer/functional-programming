/*
tomato: 7,000 won
orange: 15,000 won
apple: 10,000 won
 */
export let totalPrice = 0;
totalPrice += 7000;
totalPrice += 15000;
totalPrice += 10000;
totalPrice += 30000;    // typo? orange * 2 ? apple * 3 ?

totalPrice += 8000;     // typo!
totalPrice += 70000;    // typo!
